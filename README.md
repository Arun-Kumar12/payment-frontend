# Required softwares
1. Python 3.10+
2. Node 22.13
3. MongoDB 8.0.4

# Install all the node modules with the following command
npm install

# Install angular CLI
npm install -g @angular/cli

# To start the application run the follwoing command
ng serve